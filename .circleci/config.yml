# .circleci/config.yml
version: 2.1

jobs:
  test-arm:
    machine:
      image: ubuntu-2004:202101-01
    resource_class: arm.medium
    steps:
      - checkout
      - run: uname -a
      - run: sudo apt install meson git-lfs ninja-build
      - run: meson -Ddev_build=true build
      - run: cd build
      - run: ninja -C project/build test

workflows:
  build:
    jobs:
      - test-arm